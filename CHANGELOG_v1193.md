# JJ Arena v1.19.3

## PIN self-service

- プレイヤーのアカウント設定に「ログインPIN」セキュリティ枠を追加。
- 現在PIN、新PIN、新PIN確認の3項目で本人が変更できる。
- 既存 `/api/auth/change-pin` を利用し、現在PINの照合を必須とする。
- 変更後は現在端末以外のセッションを失効する。

## Administrator PIN verification

- 管理画面から候補の6桁PINを照合できる管理者専用APIを追加。
- PINそのものやpassword hashは返却しない。
- 照合結果は一致 / 不一致のみ。
- 同一管理者・同一対象の不一致試行は10分間に5回まで。
- 監査ログには候補PINを保存せず、一致 / 不一致だけを記録する。
- 削除済みアカウントは照合不可。
- 既存の管理者PINリセット機能は維持する。

## Security decision

PINはPBKDF2-SHA256の一方向ハッシュとして保持するため、管理者にも現在PINを平文表示しない。管理者が必要な場合は、プレイヤーから申告された候補PINの照合、またはPINリセットを使用する。
